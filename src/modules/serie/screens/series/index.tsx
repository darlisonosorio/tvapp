import React from 'react';
import { AppScreen, Typography } from '../../../../components';

const SeriesScreen = () => {
  return (
    <AppScreen
      title="Series"
      enableBackAction={false}
    >
      <Typography>SERIES PAGE CONTENT...</Typography>
    </AppScreen>
  );
};

export default SeriesScreen;